# One protected local monitor directory per environment and immutable source label.
NCF_BACKUP_MONITOR_SOURCE=
# Approved receiver: accept version 1 JSON, deduplicate its id, acknowledge only durable acceptance.
NCF_BACKUP_NOTIFY_URL=
NCF_BACKUP_NOTIFY_TOKEN=
PATH=/opt/node/bin:/usr/local/bin:/usr/bin:/bin
