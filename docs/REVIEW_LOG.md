# Next-cloud-flare 設計レビュー履歴

設計書 `docs/DESIGN.md` を GPT-6 Astra（レビュアー）と GPT-5.6 Sol（改訂・実装）で往復した記録。
各ラウンドの原文は `docs/reviews/roundN-astra.md`、反映表は `docs/reviews/roundN-resolution.md`。

| ラウンド | 観点 | レビュアー / モデル | 判定 | 設計版 | 主な変更 |
|---|---|---|---|---|---|
| 1 | 通常設計レビュー | Astra `gpt-6-astra-high` | 実装非推奨 (Blocker 15 / Major 17 / Minor 16) | v0.1 → v0.2 | 不変 blob + `nodes.current_blob_id/revision`、D1 を正本化・バックアップ/Time Travel 手順、root node と `name_ci` 一意制約、クォータ予約、`trash_ops` 削除操作単位の回収箱、LockDO(space) + `fsMutation` 共通境界、UploadDO 状態機械、principal/capability 権限表、公開パス `/api/v1/public/*` と Bypass 一致、プロトコル別 CSRF・用途別 CSP、サイズ上限再定義（95 MB / 64 MiB part / 10,000 parts）、Images 20 MB・WASM 別予算、ZIP v1 上限、Cron `SUN` + D1 lease、owner bootstrap `OWNER_EMAILS`、identity = iss+sub、outbox 付きサムネイル Queue、bigram 検索テーブル分離、機能マトリクス (v1/v1.1/非目標)、障害境界テスト |
| 2 | 敵対的レビュー (red team) | Astra `gpt-6-astra-high` | No (P0 9 件 / P1 4 件) | v0.2 → v0.3 | fsMutation 疑似コード・operation claim・影響行検証、`tree_generation` による構造変更直列化、trash_ops 排他状態機械と FK 削除順序、GC 不可逆点と猶予 35 日、ControlDO epoch による復旧時の旧 job/ticket 無効化、物理容量台帳 `physical_bytes`、UploadDO 失敗終端と barrier、operand 全域の認可と ticket の node 束縛、upload-only 自動リネーム、node_id 単位ロック + owner 限定 UNLOCK、collection ETag に node_id、ZIP entry 1,000 上限、D1 容量攻撃上限、費用冪等 |
| 3 | セキュリティ / 認証・認可 | Astra `gpt-6-astra-high` | No (P0 9 件 / P1 3 件) | v0.3 → v0.4 | JWT 受入プロファイル（ヘッダのみ・RS256・route 別 aud・JWKS 更新規則・user/service principal 分離）、失効伝播の上限時間、宣言的 route manifest + CI 被覆検査、read でも祖先有効性検査、`control.epoch` を D1 batch 条件へ複製、トークン仕様表・unlock Cookie 属性・公開 JSON API の Origin 検証、コンテンツ配信 matrix（CONTENT_HOST / 単一 host）、bounded parser 上限表、鍵ローテーション手順、share secret を URL fragment 化（自動ログ漏えい対策）、bootstrap 一回性。**機能追加**: ギャラリー / 本棚 (EPUB・ZIP/CBZ) / オーディオライブラリ (§9A) |
| 4 | Cloudflare 制約・実装可能性 | Astra `gpt-6-astra-high` | 実現可能 / 確定契約としては No (P0 8 件 / P1 5 件) | v0.4 → v0.5 | fsMutation の実 SQL（claim / proof 条件 / final barrier / commit 不明時の再読み / LockDO fence）、UploadDO・trash_ops 状態遷移表の統一、content-session Cookie による CONTENT_HOST ticket 搬送、EPUB reader を信頼 shell + sandbox srcdoc の二重 iframe 化（サーバ sanitize）、共有 landing 用公開バンドル、WebDAV Class 2 を RFC §6.4 準拠（lock creator 一致）へ、XML fixture と fast-xml-parser adapter 契約、PBKDF2 100,000 回既定 + staging gate、`crypto.DigestStream` 採用（hash-wasm 不採用）、fflate 同期 API のみ、FTS を除く logical backup + 再構築手順、制限値の一次資料照合と error mapping、D1 query/rows 予算、DO 容量・alarm・stale 表、CI 三段階、完全 route manifest、配備可能な wrangler.jsonc、30 deliverable への phase 再分割 |
| 5 | 最終判定・Sol 引き渡し | Astra `GPT-6 Astra` | No-Go (G01〜G11) → v0.6 で是正 | v0.5 → v0.6 | `_assert` SQL-error barrier + Phase 0 `changes()` gate/EXISTS fallback、D1 open/revoked permit と commit時失効 assertion、commit不明の Operation-Id 照合、ControlDO単独 epoch 発行、journal廃止と Time Travel 30日 + watermark付き日次 logical export（最大35日・最少5世代）、`trash_members` と全chunk/FK/GC fence、private/public single upload 3-route・staging/orphan物理課金・late part全abort・R2 7日lifecycle、owner allowlist bootstrap・最後のadmin保護・用途別key rotation/失効表、CSRF profile付き完全route manifest・単一operation表、client thumbnail v1無効化、BudgetDO/content_sessions・累積費用/非開示/STORE ZIP、external-content bigram FTS/substring・Gallery上限、DAV protocol profile、Gallery/Bookshelf/Audio E2E、Phase 0→1 Implementation Brief |

## 運用メモ

- レビューは Devin CLI `devin -p --model <model> --permission-mode dangerous --prompt-file docs/reviews/roundN-prompt.md` で実行。
- 改訂は Sol (`gpt-5-6-sol-xhigh`) に `docs/reviews/roundN-revise-prompt.md`（確定方針付き）で委譲。
